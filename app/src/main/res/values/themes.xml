<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Noted" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white_background</item>
        <item name="colorPrimaryDark">@color/white_background</item>
        <item name="colorAccent">@color/white_background</item>
        <item name="background">@color/white_background</item>
        <item name="buttonStyle">@color/light_blue</item>
        <item name="textBackground">@color/dark_background</item>
        <item name="editTextBackground">@color/light_field_stable</item>
        <item name="editTextColor">@color/light_field_pressed</item>
        <item name="colorBackgroundFloating">@color/text_light_theme_color</item>
        <item name="carousel_firstView">@color/text_dark_theme_color</item>

        <!-- Customize your theme here. -->
    </style>
    <style name="Launchtheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splashtheme</item>
    </style>

</resources>